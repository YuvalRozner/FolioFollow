rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All data access goes through the backend Cloud Function (Admin SDK),
    // which bypasses security rules. Direct client access is denied.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
